apiVersion: v1
kind: Service
metadata:
  name: auth
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: auth

  ports:
    - name: http
      port: 3001
      targetPort: 3001

  type: ClusterIP